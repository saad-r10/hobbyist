{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && cd server && npm install && npx prisma generate --schema=./prisma/schema.production.prisma"
  },
  "deploy": {
    "preDeployCommand": "cd server && npx prisma db push --schema=./prisma/schema.production.prisma",
    "startCommand": "cd server && node src/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
